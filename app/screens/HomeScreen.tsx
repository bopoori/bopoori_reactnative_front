import React from "react"
import { Screen, Text } from "app/components"

export const HomeScreen = () => {
  return (
    <Screen preset="scroll">
      <Text tx="navigator.homeTab" size="lg" />
    </Screen>
  )
}
